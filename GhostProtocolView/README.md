# Building the Unity project for viewing the window view

* Install Unity Hub, sign up to personal account

* open GhostProtocolView project in a recent Unity Release (e.g. 6.4)

* in package manager (window -> package manager) install NativeWebSocket via github link:
https://github.com/endel/NativeWebSocket.git#upm